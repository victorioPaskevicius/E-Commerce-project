import {db} from "../config/db.js";

// 🧩 7. 📈 Administración (Dashboard)
// Pueden ser los mismos endpoints de arriba con control de acceso (auth + role = admin).
// GET	/admin/stats	Obtener estadísticas generales: total ventas, productos más vendidos, etc.
